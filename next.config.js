/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // basePath: make sure to also update <base href="..." /> in index.tsx
  basePath: "/audiate",
};

module.exports = nextConfig;
